#!/usr/bin/env node
const fs = require("fs"); const path = require("path"); const root = process.cwd(); const dist = path.join(root, "dist"); function copyFile(from,to){fs.mkdirSync(path.dirname(to),{recursive:true});fs.copyFileSync(from,to)} if(fs.existsSync(dist)) fs.rmSync(dist,{recursive:true,force:true}); fs.mkdirSync(dist,{recursive:true}); for(const file of fs.readdirSync(root)){const src=path.join(root,file); if(fs.statSync(src).isFile()&&(file.endsWith(".html")||file.endsWith(".css")||file.endsWith(".js")||file.endsWith(".svg")||file.endsWith(".txt")||file.endsWith(".xml"))) copyFile(src,path.join(dist,file));} console.log("Static build completed. Output directory: dist");